# v1.6.1

- 新增只指向 `YUIAOI7592/BiliCDN_TW` 最新正式 GitHub Release 的 `@updateURL` 與 `@downloadURL`。
- 新增本儲存庫首頁與 Issues 連結；不加入 `@require` 或其他遠端程式碼來源。
- GitHub Release 僅上傳單一 `BiliCDN_TW.user.js`；建置報告、patch 與 checksum 只留在 Git 儲存庫。
- 播放、2x、AV1、CDN 選路、Watchdog、診斷與控制中心邏輯均未變更。
- v1.6.0 因沒有更新網址，必須手動安裝 v1.6.1 一次；之後才會自動跟隨本儲存庫正式版本。
